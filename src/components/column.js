import React from 'react';
import Col from '../containers/column';

// import Note from './note';

// eslint-disable-next-line react/prefer-stateless-function
const Column = (props) => {
  return <div>{Col}</div>;
};

export default Column;
